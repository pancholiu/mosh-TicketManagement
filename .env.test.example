PORT=3000
# Use a separate database for E2E tests — never point this at your dev database
DATABASE_URL=postgresql://user:password@localhost:5432/ticketdb_test
# Minimum 32 characters. Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=test_only_secret_change_before_running_tests_1234
BETTER_AUTH_URL=http://localhost:3000
CLIENT_ORIGIN=http://localhost:5173
ADMIN_EMAIL=admin@test.example.com
# Minimum 16 characters. Generate with: openssl rand -base64 16
ADMIN_PASSWORD=test_admin_password_123
